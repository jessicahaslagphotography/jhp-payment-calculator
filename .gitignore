photos/

# Before/after screenshots for the 24 Sep heading pass. Artifacts, not source --
# 2MB of PNGs that can be regenerated from the templates at any time.
headings/
__pycache__/
*.pyc

# The static bundle deploy-site.py assembles. A build artefact: 857KB of
# generated HTML that is the eight scalogy-*.html files plus twelve Jinja
# renders, so committing it would put every launch edit in the diff twice. The
# host builds it on each push -- see netlify.toml.
public/
